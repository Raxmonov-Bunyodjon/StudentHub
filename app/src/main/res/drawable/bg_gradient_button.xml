<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 🔹 Gradient background for the button -->
    <gradient
        android:startColor="@color/text_input_stroke"
        android:endColor="@color/teal_200"
        android:angle="45"/>

    <!-- 🔹 Rounded corners with 24dp radius -->
    <corners android:radius="24dp"/>

    <!-- 🔹 Inner padding to create spacing inside the shape -->
    <padding
        android:left="16dp"
        android:top="12dp"
        android:right="16dp"
        android:bottom="12dp"/>
    </shape>