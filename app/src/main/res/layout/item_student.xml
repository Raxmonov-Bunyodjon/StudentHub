<?xml version="1.0" encoding="utf-8"?>
<!--
🔹 item_student.xml

This layout file is used for a single item (card) in the list of students.
Each item displays the student's name, faculty, and direction, along with edit/delete actions.

Elements:
1. CardView → visual card container with shadow and corner radius.
2. LinearLayout → horizontal container for inner elements.
3. Left LinearLayout → student details (name, faculty, direction)
4. Right LinearLayout → Edit and Delete buttons.
-->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/tools"
    android:layout_margin="8dp"
    app:boxBackgroundMode="outline"
    app:boxStrokeColor="@color/text_input_stroke"
    app:boxStrokeWidth="2dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:ignore="NamespaceTypo"
    app:shapeAppearanceOverlay="@style/RoundedCornerTextField"
    app:strokeColor="@color/colorPrimary">

    <!-- 🔹 Inner horizontal container -->
    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp"
        android:gravity="center_vertical">

        <!-- 🔹 Left side: Student details -->
        <androidx.appcompat.widget.LinearLayoutCompat
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvStudentName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Full Name"
                android:textColor="@android:color/black"
                android:textSize="18sp"
                android:textStyle="bold"/>

            <TextView
                android:id="@+id/tvStudentFaculty"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Faculty"
                android:textColor="#FF00F2"
                android:textSize="14sp" />

            <TextView
                android:id="@+id/tvStudentDirection"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Direction"
                android:textColor="#FF8400"
                android:textSize="14sp" />
        </androidx.appcompat.widget.LinearLayoutCompat>

        <!-- 🔹 Right side: Edit and Delete buttons -->
        <androidx.appcompat.widget.LinearLayoutCompat
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end">

            <ImageButton
                android:id="@+id/btnEditStudent"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Edit Student"
                android:src="@drawable/outline_edit_24"
                card_view:tint = "@color/black"/>

            <ImageView
                android:id="@+id/btnDeleteStudent"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:padding="5dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Delete Student"
                android:src="@drawable/outline_delete_24"
                card_view:tint = "@color/red"/>
        </androidx.appcompat.widget.LinearLayoutCompat>
    </androidx.appcompat.widget.LinearLayoutCompat>


</com.google.android.material.card.MaterialCardView>